# Sankey v2 -- the flow template sensors.
#
# GENERATED by scratchpad/gen_flow_yaml_v2.py. Do not hand-edit: the Jinja
# preamble is repeated verbatim twelve times and test_jinja_parity.py asserts
# all twelve copies are byte-identical.
#
# This file is the SOURCE OF TRUTH for the Jinja. It is consumed by
#   - sankey_tests/test_jinja_parity.py    (renders it, compares to flows.py)
#   - scratchpad/create_flow_helpers_v2.py (feeds it to the template config flow)
# It is NOT a /config/configuration.yaml fragment -- there is no write path to
# /config from the dev machine, so the sensors are created as config-entry
# helpers instead. The YAML shape is kept anyway because it is the readable form.
#
# ----------------------------------------------------------- the three stages
# Each of the twelve flows is a chain of THREE helpers:
#
#   flow_<s>_to_<t>_power    template, W      MAY go unavailable
#   flow_<s>_to_<t>_energy   integration, kWh (Riemann, left, max_sub 60 s)
#   flow_<s>_to_<t>_daily    utility_meter, daily cycle   <- the card's link value
#
# A FOURTH never-unavailable wrapper stage was built, measured, and CANCELLED
# on 2026-08-30. It is worth recording why, because the reasoning is not
# obvious and someone will propose it again.
#
# Unavailability really does propagate the whole length of the chain. Measured
# live by flipping one power template's availability to false:
#     before   template=369.6494    integration=0.063        meter=0.062
#     t+10s    template=unavailable integration=unavailable  meter=unavailable
#     t+130s   template=unavailable integration=unavailable  meter=unavailable
#     restored template=397.4359    integration=0.063        meter=0.062
# Nothing was invented or lost across the gap, but the meter does NOT hold.
#
# That looked fatal, because an unavailable link `value:` parses to NaN and
# Math.min NaN-poisons the source's spent total, silently zeroing every LATER
# ribbon from that source. It is not fatal, because the card never reads the
# live state on this path: with energy_date_selection: true the link `value:`
# goes through the SAME Recorder statistics substitution as a node's state
# (bundle chars 100603 -> 96800 -> 96959/97572/70176), so the value is
# sum(change), not hass.states. The substitution only needs the entity to
# EXIST in hass.states, and it does -- measured, the meter stays present and
# merely reads 'unavailable':
#     before/during/t+75s/restored : present=True, state 0.032 / unavailable /
#                                    unavailable / 0.033
#
# So the wrapper would have bought nothing while costing a `this.state`
# self-reference, twelve more first-statistics-rows carrying change = 0, and a
# midnight stale-hold hazard. What the value: target MUST have is a
# state_class, because an entity with no statistics rows returns null from
# `ki`, which stringifies to "null" and parses to NaN. A utility_meter carries
# total_increasing natively, which is exactly why pointing at it directly is
# the safe option. Verified: all 36 flow ids appear in
# recorder/list_statistic_ids, and the meters compile rows.
#
# ---------------------------------------------------------------- conventions
# Sign conventions are Home Assistant's, NOT the raw Modbus registers':
#   sensor.solis_inverter_solar_power     W, DC, >= 0
#   sensor.solis_inverter_battery_power   W, DC, POSITIVE = CHARGING
#   sensor.solis_inverter_grid_power      W, AC, POSITIVE = IMPORTING
#   sensor.solis_inverter_house_load      W, AC, >= 0, INCLUDES the Tesla
#   sensor.tesla_home_charging_power      W, AC, >= 0
#
# Entity names use v2-layout's vocabulary, which names a sink after its NODE:
# `grid` as a sink is EXPORT, `battery` as a sink is CHARGE. So flows.py's
# `solar_to_export` is the entity `flow_solar_to_grid_*`. The mapping lives in
# the generator and in test_jinja_parity.SLUG, and is asserted by a test.
#
# ------------------------------------------------------------------ the model
# BRIEF_V2.md section 2, proportional attribution with solar-only export:
#
#   S  = max(0, solar)              B = max(0, -battery)   C = max(0, battery)
#   G  = max(0, grid)               E = max(0, -grid)
#   T  = min(max(0, tesla), H)      Hr = max(0, H - T)
#   L  = max(0, (S + B + G) - (Hr + T + C + E))          <- the Inverter node
#
#   step 1  export is structurally solar-only:  s2e = min(S, E);  S1 = S - s2e
#   step 2  every remaining sink draws the same source mix:
#             w = S1/tot, b = B/tot, g = G/tot   with tot = S1 + B + G
#           and 0 for all three when tot == 0 (the zero-supply guard, exactly
#           `tot > 0`, no epsilon -- flows.py uses `total <= 0.0` and v1's
#           epsilon trim is not being repeated).
#
# There is no battery -> export link and never will be: all three discharge
# windows are unset. There is no battery -> battery link either; the battery
# cannot charge and discharge in the same sample, so B > 0 implies C == 0.
#
# ------------------------------------------------------------- unavailability
# The POWER sensors require ALL FIVE sources to be real numbers within
# +/-100 kW. `| float` carries NO default, so a non-number raises and the
# sensor renders `unavailable` rather than banking a silent 0 W -- the
# `| float(0)` trap, where a dropout is banked as real zero energy.
#
# Every one of the twelve power sensors depends on the Tesla reading (Hr is
# derived from it), so TeslaMate going away blanks all twelve. Measured: the
# Tesla power sensor never went non-numeric on any fixture day -- a sleeping
# car reads a genuine 0 W, because the template gates on current > 0 -- so the
# 73%-offline figure in CLAUDE.md does NOT translate into blanking.
#
# Values are rounded to 4 dp of a watt: worst-case error 5e-5 W, inside the
# 1e-4 W parity gate.

template:
  - sensor:
      # ------- solar_to_grid  (flows.py key: solar_to_export) -------
      - name: "Flow solar to grid power"
        unique_id: flow_solar_to_grid_power
        unit_of_measurement: W
        device_class: power
        state_class: measurement
        availability: &v2_flow_avail >
          {% set vs = [ 'sensor.solis_inverter_solar_power',
                        'sensor.solis_inverter_battery_power',
                        'sensor.solis_inverter_grid_power',
                        'sensor.solis_inverter_house_load',
                        'sensor.tesla_home_charging_power' ]
                      | map('states') | select('is_number') | map('float')
                      | map('abs') | list %}
          {{ vs | count == 5 and (vs | max) <= 100000 }}
        state: >
          {% set S  = [ 0, states('sensor.solis_inverter_solar_power')   | float ] | max %}
          {% set BP =      states('sensor.solis_inverter_battery_power') | float %}
          {% set GP =      states('sensor.solis_inverter_grid_power')    | float %}
          {% set H  = [ 0, states('sensor.solis_inverter_house_load')    | float ] | max %}
          {% set TR = [ 0, states('sensor.tesla_home_charging_power')    | float ] | max %}
          {% set B  = [ 0, -BP ] | max %}
          {% set C  = [ 0,  BP ] | max %}
          {% set G  = [ 0,  GP ] | max %}
          {% set E  = [ 0, -GP ] | max %}
          {% set T  = [ TR, H ] | min %}
          {% set Hr = [ 0, H - T ] | max %}
          {% set L  = [ 0, (S + B + G) - (Hr + T + C + E) ] | max %}
          {% set s2e = [ S, E ] | min %}
          {% set S1 = S - s2e %}
          {% set tot = S1 + B + G %}
          {% set w = S1 / tot if tot > 0 else 0 %}
          {% set b = B  / tot if tot > 0 else 0 %}
          {% set g = G  / tot if tot > 0 else 0 %}
          {{ (s2e) | round(4) }}

      # ------- solar_to_battery  (flows.py key: solar_to_battery) -------
      - name: "Flow solar to battery power"
        unique_id: flow_solar_to_battery_power
        unit_of_measurement: W
        device_class: power
        state_class: measurement
        availability: *v2_flow_avail
        state: >
          {% set S  = [ 0, states('sensor.solis_inverter_solar_power')   | float ] | max %}
          {% set BP =      states('sensor.solis_inverter_battery_power') | float %}
          {% set GP =      states('sensor.solis_inverter_grid_power')    | float %}
          {% set H  = [ 0, states('sensor.solis_inverter_house_load')    | float ] | max %}
          {% set TR = [ 0, states('sensor.tesla_home_charging_power')    | float ] | max %}
          {% set B  = [ 0, -BP ] | max %}
          {% set C  = [ 0,  BP ] | max %}
          {% set G  = [ 0,  GP ] | max %}
          {% set E  = [ 0, -GP ] | max %}
          {% set T  = [ TR, H ] | min %}
          {% set Hr = [ 0, H - T ] | max %}
          {% set L  = [ 0, (S + B + G) - (Hr + T + C + E) ] | max %}
          {% set s2e = [ S, E ] | min %}
          {% set S1 = S - s2e %}
          {% set tot = S1 + B + G %}
          {% set w = S1 / tot if tot > 0 else 0 %}
          {% set b = B  / tot if tot > 0 else 0 %}
          {% set g = G  / tot if tot > 0 else 0 %}
          {{ (C * w) | round(4) }}

      # ------- solar_to_house  (flows.py key: solar_to_house) -------
      - name: "Flow solar to house power"
        unique_id: flow_solar_to_house_power
        unit_of_measurement: W
        device_class: power
        state_class: measurement
        availability: *v2_flow_avail
        state: >
          {% set S  = [ 0, states('sensor.solis_inverter_solar_power')   | float ] | max %}
          {% set BP =      states('sensor.solis_inverter_battery_power') | float %}
          {% set GP =      states('sensor.solis_inverter_grid_power')    | float %}
          {% set H  = [ 0, states('sensor.solis_inverter_house_load')    | float ] | max %}
          {% set TR = [ 0, states('sensor.tesla_home_charging_power')    | float ] | max %}
          {% set B  = [ 0, -BP ] | max %}
          {% set C  = [ 0,  BP ] | max %}
          {% set G  = [ 0,  GP ] | max %}
          {% set E  = [ 0, -GP ] | max %}
          {% set T  = [ TR, H ] | min %}
          {% set Hr = [ 0, H - T ] | max %}
          {% set L  = [ 0, (S + B + G) - (Hr + T + C + E) ] | max %}
          {% set s2e = [ S, E ] | min %}
          {% set S1 = S - s2e %}
          {% set tot = S1 + B + G %}
          {% set w = S1 / tot if tot > 0 else 0 %}
          {% set b = B  / tot if tot > 0 else 0 %}
          {% set g = G  / tot if tot > 0 else 0 %}
          {{ (Hr * w) | round(4) }}

      # ------- solar_to_tesla  (flows.py key: solar_to_tesla) -------
      - name: "Flow solar to tesla power"
        unique_id: flow_solar_to_tesla_power
        unit_of_measurement: W
        device_class: power
        state_class: measurement
        availability: *v2_flow_avail
        state: >
          {% set S  = [ 0, states('sensor.solis_inverter_solar_power')   | float ] | max %}
          {% set BP =      states('sensor.solis_inverter_battery_power') | float %}
          {% set GP =      states('sensor.solis_inverter_grid_power')    | float %}
          {% set H  = [ 0, states('sensor.solis_inverter_house_load')    | float ] | max %}
          {% set TR = [ 0, states('sensor.tesla_home_charging_power')    | float ] | max %}
          {% set B  = [ 0, -BP ] | max %}
          {% set C  = [ 0,  BP ] | max %}
          {% set G  = [ 0,  GP ] | max %}
          {% set E  = [ 0, -GP ] | max %}
          {% set T  = [ TR, H ] | min %}
          {% set Hr = [ 0, H - T ] | max %}
          {% set L  = [ 0, (S + B + G) - (Hr + T + C + E) ] | max %}
          {% set s2e = [ S, E ] | min %}
          {% set S1 = S - s2e %}
          {% set tot = S1 + B + G %}
          {% set w = S1 / tot if tot > 0 else 0 %}
          {% set b = B  / tot if tot > 0 else 0 %}
          {% set g = G  / tot if tot > 0 else 0 %}
          {{ (T * w) | round(4) }}

      # ------- solar_to_inverter  (flows.py key: solar_to_inverter) -------
      - name: "Flow solar to inverter power"
        unique_id: flow_solar_to_inverter_power
        unit_of_measurement: W
        device_class: power
        state_class: measurement
        availability: *v2_flow_avail
        state: >
          {% set S  = [ 0, states('sensor.solis_inverter_solar_power')   | float ] | max %}
          {% set BP =      states('sensor.solis_inverter_battery_power') | float %}
          {% set GP =      states('sensor.solis_inverter_grid_power')    | float %}
          {% set H  = [ 0, states('sensor.solis_inverter_house_load')    | float ] | max %}
          {% set TR = [ 0, states('sensor.tesla_home_charging_power')    | float ] | max %}
          {% set B  = [ 0, -BP ] | max %}
          {% set C  = [ 0,  BP ] | max %}
          {% set G  = [ 0,  GP ] | max %}
          {% set E  = [ 0, -GP ] | max %}
          {% set T  = [ TR, H ] | min %}
          {% set Hr = [ 0, H - T ] | max %}
          {% set L  = [ 0, (S + B + G) - (Hr + T + C + E) ] | max %}
          {% set s2e = [ S, E ] | min %}
          {% set S1 = S - s2e %}
          {% set tot = S1 + B + G %}
          {% set w = S1 / tot if tot > 0 else 0 %}
          {% set b = B  / tot if tot > 0 else 0 %}
          {% set g = G  / tot if tot > 0 else 0 %}
          {{ (L * w) | round(4) }}

      # ------- battery_to_house  (flows.py key: battery_to_house) -------
      - name: "Flow battery to house power"
        unique_id: flow_battery_to_house_power
        unit_of_measurement: W
        device_class: power
        state_class: measurement
        availability: *v2_flow_avail
        state: >
          {% set S  = [ 0, states('sensor.solis_inverter_solar_power')   | float ] | max %}
          {% set BP =      states('sensor.solis_inverter_battery_power') | float %}
          {% set GP =      states('sensor.solis_inverter_grid_power')    | float %}
          {% set H  = [ 0, states('sensor.solis_inverter_house_load')    | float ] | max %}
          {% set TR = [ 0, states('sensor.tesla_home_charging_power')    | float ] | max %}
          {% set B  = [ 0, -BP ] | max %}
          {% set C  = [ 0,  BP ] | max %}
          {% set G  = [ 0,  GP ] | max %}
          {% set E  = [ 0, -GP ] | max %}
          {% set T  = [ TR, H ] | min %}
          {% set Hr = [ 0, H - T ] | max %}
          {% set L  = [ 0, (S + B + G) - (Hr + T + C + E) ] | max %}
          {% set s2e = [ S, E ] | min %}
          {% set S1 = S - s2e %}
          {% set tot = S1 + B + G %}
          {% set w = S1 / tot if tot > 0 else 0 %}
          {% set b = B  / tot if tot > 0 else 0 %}
          {% set g = G  / tot if tot > 0 else 0 %}
          {{ (Hr * b) | round(4) }}

      # ------- battery_to_tesla  (flows.py key: battery_to_tesla) -------
      - name: "Flow battery to tesla power"
        unique_id: flow_battery_to_tesla_power
        unit_of_measurement: W
        device_class: power
        state_class: measurement
        availability: *v2_flow_avail
        state: >
          {% set S  = [ 0, states('sensor.solis_inverter_solar_power')   | float ] | max %}
          {% set BP =      states('sensor.solis_inverter_battery_power') | float %}
          {% set GP =      states('sensor.solis_inverter_grid_power')    | float %}
          {% set H  = [ 0, states('sensor.solis_inverter_house_load')    | float ] | max %}
          {% set TR = [ 0, states('sensor.tesla_home_charging_power')    | float ] | max %}
          {% set B  = [ 0, -BP ] | max %}
          {% set C  = [ 0,  BP ] | max %}
          {% set G  = [ 0,  GP ] | max %}
          {% set E  = [ 0, -GP ] | max %}
          {% set T  = [ TR, H ] | min %}
          {% set Hr = [ 0, H - T ] | max %}
          {% set L  = [ 0, (S + B + G) - (Hr + T + C + E) ] | max %}
          {% set s2e = [ S, E ] | min %}
          {% set S1 = S - s2e %}
          {% set tot = S1 + B + G %}
          {% set w = S1 / tot if tot > 0 else 0 %}
          {% set b = B  / tot if tot > 0 else 0 %}
          {% set g = G  / tot if tot > 0 else 0 %}
          {{ (T * b) | round(4) }}

      # ------- battery_to_inverter  (flows.py key: battery_to_inverter) -------
      - name: "Flow battery to inverter power"
        unique_id: flow_battery_to_inverter_power
        unit_of_measurement: W
        device_class: power
        state_class: measurement
        availability: *v2_flow_avail
        state: >
          {% set S  = [ 0, states('sensor.solis_inverter_solar_power')   | float ] | max %}
          {% set BP =      states('sensor.solis_inverter_battery_power') | float %}
          {% set GP =      states('sensor.solis_inverter_grid_power')    | float %}
          {% set H  = [ 0, states('sensor.solis_inverter_house_load')    | float ] | max %}
          {% set TR = [ 0, states('sensor.tesla_home_charging_power')    | float ] | max %}
          {% set B  = [ 0, -BP ] | max %}
          {% set C  = [ 0,  BP ] | max %}
          {% set G  = [ 0,  GP ] | max %}
          {% set E  = [ 0, -GP ] | max %}
          {% set T  = [ TR, H ] | min %}
          {% set Hr = [ 0, H - T ] | max %}
          {% set L  = [ 0, (S + B + G) - (Hr + T + C + E) ] | max %}
          {% set s2e = [ S, E ] | min %}
          {% set S1 = S - s2e %}
          {% set tot = S1 + B + G %}
          {% set w = S1 / tot if tot > 0 else 0 %}
          {% set b = B  / tot if tot > 0 else 0 %}
          {% set g = G  / tot if tot > 0 else 0 %}
          {{ (L * b) | round(4) }}

      # ------- grid_to_house  (flows.py key: grid_to_house) -------
      - name: "Flow grid to house power"
        unique_id: flow_grid_to_house_power
        unit_of_measurement: W
        device_class: power
        state_class: measurement
        availability: *v2_flow_avail
        state: >
          {% set S  = [ 0, states('sensor.solis_inverter_solar_power')   | float ] | max %}
          {% set BP =      states('sensor.solis_inverter_battery_power') | float %}
          {% set GP =      states('sensor.solis_inverter_grid_power')    | float %}
          {% set H  = [ 0, states('sensor.solis_inverter_house_load')    | float ] | max %}
          {% set TR = [ 0, states('sensor.tesla_home_charging_power')    | float ] | max %}
          {% set B  = [ 0, -BP ] | max %}
          {% set C  = [ 0,  BP ] | max %}
          {% set G  = [ 0,  GP ] | max %}
          {% set E  = [ 0, -GP ] | max %}
          {% set T  = [ TR, H ] | min %}
          {% set Hr = [ 0, H - T ] | max %}
          {% set L  = [ 0, (S + B + G) - (Hr + T + C + E) ] | max %}
          {% set s2e = [ S, E ] | min %}
          {% set S1 = S - s2e %}
          {% set tot = S1 + B + G %}
          {% set w = S1 / tot if tot > 0 else 0 %}
          {% set b = B  / tot if tot > 0 else 0 %}
          {% set g = G  / tot if tot > 0 else 0 %}
          {{ (Hr * g) | round(4) }}

      # ------- grid_to_tesla  (flows.py key: grid_to_tesla) -------
      - name: "Flow grid to tesla power"
        unique_id: flow_grid_to_tesla_power
        unit_of_measurement: W
        device_class: power
        state_class: measurement
        availability: *v2_flow_avail
        state: >
          {% set S  = [ 0, states('sensor.solis_inverter_solar_power')   | float ] | max %}
          {% set BP =      states('sensor.solis_inverter_battery_power') | float %}
          {% set GP =      states('sensor.solis_inverter_grid_power')    | float %}
          {% set H  = [ 0, states('sensor.solis_inverter_house_load')    | float ] | max %}
          {% set TR = [ 0, states('sensor.tesla_home_charging_power')    | float ] | max %}
          {% set B  = [ 0, -BP ] | max %}
          {% set C  = [ 0,  BP ] | max %}
          {% set G  = [ 0,  GP ] | max %}
          {% set E  = [ 0, -GP ] | max %}
          {% set T  = [ TR, H ] | min %}
          {% set Hr = [ 0, H - T ] | max %}
          {% set L  = [ 0, (S + B + G) - (Hr + T + C + E) ] | max %}
          {% set s2e = [ S, E ] | min %}
          {% set S1 = S - s2e %}
          {% set tot = S1 + B + G %}
          {% set w = S1 / tot if tot > 0 else 0 %}
          {% set b = B  / tot if tot > 0 else 0 %}
          {% set g = G  / tot if tot > 0 else 0 %}
          {{ (T * g) | round(4) }}

      # ------- grid_to_battery  (flows.py key: grid_to_battery) -------
      - name: "Flow grid to battery power"
        unique_id: flow_grid_to_battery_power
        unit_of_measurement: W
        device_class: power
        state_class: measurement
        availability: *v2_flow_avail
        state: >
          {% set S  = [ 0, states('sensor.solis_inverter_solar_power')   | float ] | max %}
          {% set BP =      states('sensor.solis_inverter_battery_power') | float %}
          {% set GP =      states('sensor.solis_inverter_grid_power')    | float %}
          {% set H  = [ 0, states('sensor.solis_inverter_house_load')    | float ] | max %}
          {% set TR = [ 0, states('sensor.tesla_home_charging_power')    | float ] | max %}
          {% set B  = [ 0, -BP ] | max %}
          {% set C  = [ 0,  BP ] | max %}
          {% set G  = [ 0,  GP ] | max %}
          {% set E  = [ 0, -GP ] | max %}
          {% set T  = [ TR, H ] | min %}
          {% set Hr = [ 0, H - T ] | max %}
          {% set L  = [ 0, (S + B + G) - (Hr + T + C + E) ] | max %}
          {% set s2e = [ S, E ] | min %}
          {% set S1 = S - s2e %}
          {% set tot = S1 + B + G %}
          {% set w = S1 / tot if tot > 0 else 0 %}
          {% set b = B  / tot if tot > 0 else 0 %}
          {% set g = G  / tot if tot > 0 else 0 %}
          {{ (C * g) | round(4) }}

      # ------- grid_to_inverter  (flows.py key: grid_to_inverter) -------
      - name: "Flow grid to inverter power"
        unique_id: flow_grid_to_inverter_power
        unit_of_measurement: W
        device_class: power
        state_class: measurement
        availability: *v2_flow_avail
        state: >
          {% set S  = [ 0, states('sensor.solis_inverter_solar_power')   | float ] | max %}
          {% set BP =      states('sensor.solis_inverter_battery_power') | float %}
          {% set GP =      states('sensor.solis_inverter_grid_power')    | float %}
          {% set H  = [ 0, states('sensor.solis_inverter_house_load')    | float ] | max %}
          {% set TR = [ 0, states('sensor.tesla_home_charging_power')    | float ] | max %}
          {% set B  = [ 0, -BP ] | max %}
          {% set C  = [ 0,  BP ] | max %}
          {% set G  = [ 0,  GP ] | max %}
          {% set E  = [ 0, -GP ] | max %}
          {% set T  = [ TR, H ] | min %}
          {% set Hr = [ 0, H - T ] | max %}
          {% set L  = [ 0, (S + B + G) - (Hr + T + C + E) ] | max %}
          {% set s2e = [ S, E ] | min %}
          {% set S1 = S - s2e %}
          {% set tot = S1 + B + G %}
          {% set w = S1 / tot if tot > 0 else 0 %}
          {% set b = B  / tot if tot > 0 else 0 %}
          {% set g = G  / tot if tot > 0 else 0 %}
          {{ (L * g) | round(4) }}
