const S = id => (states[id] || {}).state; const OD = Object.keys(states).filter(k => k.includes('.octopus_energy_') && k.includes('_intelligent_')).map(k => k.slice(k.indexOf('.') + 1, k.indexOf('_intelligent_'))).sort()[0]; const g = sfx => { for (const d of ['sensor','number','time','select','binary_sensor']) { const s = states[d + '.' + OD + '_intelligent_' + sfx]; if (s && s.state !== 'unavailable' && s.state !== 'unknown') { return s; } } return null; }; const st = S('sensor.tesla_state') || 'unknown'; const cs = S('sensor.tesla_charging_state'); const kw = Number(S('sensor.tesla_charger_power')); const chg = cs === 'Charging' || (isFinite(kw) && kw > 0); const plugged = S('binary_sensor.tesla_plugged_in') === 'on'; const lockedS = S('binary_sensor.tesla_locked'); const socN = Number(S('sensor.tesla_battery')); const ok = isFinite(socN); const resting = ['offline','asleep','suspended'].includes(st); const stale = S('binary_sensor.teslamate_healthy') === 'off' || ['unknown','unavailable'].includes(st) || !ok; const soc = ok ? Math.max(0, Math.min(100, socN)) : 0; const km = Number(S('sensor.tesla_range')); const mi = isFinite(km) ? Math.round(km * 0.621371) : null; const limS = g('charge_target') || states['sensor.tesla_charge_limit']; const lim = limS ? Number(limS.state) : NaN; const byS = g('target_time'); const by = byS ? String(byS.state).slice(0, 5) : null; const disp = g('dispatching'); const da = (disp && disp.attributes) || {}; const fmt = t => { const d = new Date(t); return isNaN(d) ? null : d.toTimeString().slice(0, 5); }; const plans = Array.isArray(da.planned_dispatches) ? da.planned_dispatches : []; const now = Date.now(); const raw = plans.filter(p => p && p.start && p.end && !isNaN(new Date(p.start)) && !isNaN(new Date(p.end)) && new Date(p.end).getTime() > now).sort((a, b) => new Date(a.start) - new Date(b.start)); const runs = []; for (const p of raw) { const last = runs[runs.length - 1]; if (last && new Date(p.start).getTime() - new Date(last.end).getTime() <= 60000) { if (new Date(p.end).getTime() > new Date(last.end).getTime()) { last.end = p.end; } } else { runs.push({ start: p.start, end: p.end }); } } const future = runs.slice(0, 2); if (!future.length && da.next_start && da.next_end && !isNaN(new Date(da.next_start)) && new Date(da.next_end).getTime() > now) { future.push({ start: da.next_start, end: da.next_end }); } const GREEN = 'rgb(76,175,80)'; const RED = 'rgb(244,67,54)'; const dim = 'rgba(255,255,255,0.45)'; const barCol = ok && soc <= 20 ? RED : GREEN; const asleep = resting; let l1 = ''; let l1c = dim; let l2 = ''; const ttf = Number(S('sensor.tesla_time_to_full_charge')); let left = ''; if (isFinite(ttf) && ttf > 0.02) { if (ttf < 1) { left = Math.round(ttf * 60) + ' min remaining'; } else { left = Math.floor(ttf) + ' h ' + Math.round((ttf % 1) * 60) + ' min remaining'; } } const add = Number(S('sensor.tesla_charge_energy_added')); if (stale) { l1 = 'No link to TeslaMate'; l1c = 'rgba(244,67,54,0.85)'; } else if (chg) { l1 = 'Charging' + (isFinite(kw) && kw > 0 ? ' · ' + kw.toFixed(1) + ' kW' : '') + (left ? ' · ' + left : ''); l1c = GREEN; if (isFinite(add) && isFinite(lim) && by) { l2 = '+' + add.toFixed(1) + ' kWh · → ' + Math.round(lim) + '% by ' + by; } } else if (plugged && cs === 'Complete') { l1 = 'Charge complete'; if (isFinite(add) && add > 0) { l2 = '+' + add.toFixed(1) + ' kWh added'; } } else if (plugged) { l1 = 'Plugged in' + (asleep ? ' · Asleep' : ' · Parked'); if (isFinite(lim) && by) { l2 = '→ ' + Math.round(lim) + '% by ' + by; } } else if (st === 'driving') { l1 = 'Driving'; } else if (asleep) { l1 = 'Asleep'; } else { l1 = 'Parked'; } const lockTxt = lockedS === 'on' ? 'Locked' : (lockedS === 'off' ? 'Unlocked' : ''); const lockCol = lockedS === 'off' ? 'rgba(255,193,7,0.85)' : 'rgba(255,255,255,0.40)'; const lockGlyph = lockTxt ? `<svg width='9' height='11' viewBox='0 0 10 12' style='margin-right:4px;'><rect x='1' y='5.5' width='8' height='6' rx='1.5' fill='${lockCol}'/><path d='M 3 5.5 V 3.4 A 2 2 0 0 1 7 3.4 ${lockedS === 'off' ? '' : 'V 5.5'}' fill='none' stroke='${lockCol}' stroke-width='1.4'/></svg>` : ''; const heroCol = stale ? 'rgba(255,255,255,0.55)' : 'rgba(255,255,255,0.95)'; const carCol = chg ? GREEN : 'rgba(255,255,255,0.75)'; const carGlyph = `<svg width='19' height='10' viewBox='0 0 26 13' style='flex:none;display:block;'><path d='M1.5 9 C1.5 7 2.5 6.2 4.5 5.8 C6.5 5.4 8 5.2 9.5 5.1 C10.5 3.3 12 2.3 14 2.2 C16 2.2 18 3 20.5 4.6 C22.5 5 24 5.8 24.5 7 C24.8 7.8 24.8 8.5 24.6 9' fill='none' stroke='${carCol}' stroke-width='1.6' stroke-linecap='round'/><circle cx='6.5' cy='10.5' r='1.9' fill='none' stroke='${carCol}' stroke-width='1.4'/><circle cx='19.5' cy='10.5' r='1.9' fill='none' stroke='${carCol}' stroke-width='1.4'/></svg>`; const boltG = chg ? `<svg width='7' height='10' viewBox='0 0 8 12' style='flex:none;'><path d='M4.5 0.5 L1 6.5 H3.5 L2.5 11.5 L7 5 H4 L5.5 0.5 Z' fill='${GREEN}'/></svg>` : ''; const dimCol = 'rgba(255,255,255,0.50)'; const rowH = (glyph, txt, col) => `<div style='display:flex;align-items:center;justify-content:flex-end;gap:5px;font-size:11.5px;line-height:1.3;color:${col};white-space:nowrap;font-variant-numeric:tabular-nums;'>${glyph}<span>${txt}</span></div>`; const pin = c => `<svg width='10' height='11' viewBox='0 0 12 14' style='flex:none;'><path d='M6 1 C8.8 1 11 3.2 11 6 C11 9.5 6 13 6 13 C6 13 1 9.5 1 6 C1 3.2 3.2 1 6 1 Z' fill='none' stroke='${c}' stroke-width='1.5'/><circle cx='6' cy='6' r='1.6' fill='${c}'/></svg>`; const thermo = c => `<svg width='9' height='12' viewBox='0 0 10 14' style='flex:none;'><path d='M3.5 8.2 V2.5 A1.5 1.5 0 0 1 6.5 2.5 V8.2 A3.2 3.2 0 1 1 3.5 8.2 Z' fill='none' stroke='${c}' stroke-width='1.4'/><circle cx='5' cy='10.6' r='1.4' fill='${c}'/></svg>`; const clockG = c => `<svg width='10' height='10' viewBox='0 0 12 12' style='flex:none;'><circle cx='6' cy='6' r='5' fill='none' stroke='${c}' stroke-width='1.4'/><path d='M6 3.5 V6 L8 7.5' fill='none' stroke='${c}' stroke-width='1.4' stroke-linecap='round'/></svg>`; let rows = ''; const geo = S('sensor.tesla_geofence'); if (geo && !['unknown','unavailable','none',''].includes(String(geo).trim().toLowerCase())) { rows += rowH(pin(dimCol), geo, dimCol); } const ti = Number(S('sensor.tesla_inside_temp')); if (isFinite(ti)) { rows += rowH(thermo(dimCol), ti.toFixed(1) + '°', dimCol); } if (plugged) { if (future.length) { const live = new Date(future[0].start).getTime() <= now; const scol = live ? 'rgba(76,175,80,0.95)' : dimCol; rows += rowH(clockG(scol), future.map(p => fmt(p.start) + '–' + fmt(p.end)).join(' · '), scol); } else { rows += rowH(clockG(dimCol), 'no slots', dimCol); } } const info = `<div style='display:flex;flex-direction:column;gap:4px;opacity:${stale ? 0.45 : 1};'>${rows}</div>`; const AMB = 'rgba(255,193,7,0.92)'; const BLU = 'rgba(66,165,245,0.92)'; const CYN = 'rgba(38,198,218,0.92)'; const tint = c => c.replace('0.92', '0.13'); const chipH = (glyph, txt, col) => `<span style='display:inline-flex;align-items:center;gap:5px;padding:3px 8px;border-radius:6px;background:${tint(col)};color:${col};font-size:11px;line-height:1.25;white-space:nowrap;'>${glyph}<span>${txt}</span></span>`; const gDoor = c => `<svg width='10' height='11' viewBox='0 0 12 13' style='flex:none;'><path d='M1.2 12 V3.2 L8.2 1 V12' fill='none' stroke='${c}' stroke-width='1.4' stroke-linejoin='round'/><circle cx='6.6' cy='7' r='0.9' fill='${c}'/><path d='M9.8 12 H11.2' stroke='${c}' stroke-width='1.4' stroke-linecap='round'/></svg>`; const gEye = c => `<svg width='12' height='10' viewBox='0 0 14 11' style='flex:none;'><path d='M1 5.5 C3.2 2 10.8 2 13 5.5 C10.8 9 3.2 9 1 5.5 Z' fill='none' stroke='${c}' stroke-width='1.3'/><circle cx='7' cy='5.5' r='1.7' fill='${c}'/></svg>`; const gWind = c => `<svg width='12' height='10' viewBox='0 0 13 11' style='flex:none;'><path d='M1 3 H7.4 A1.7 1.7 0 1 0 5.7 1.3' fill='none' stroke='${c}' stroke-width='1.3' stroke-linecap='round'/><path d='M1 6 H9.4 A1.7 1.7 0 1 1 7.7 7.7' fill='none' stroke='${c}' stroke-width='1.3' stroke-linecap='round'/><path d='M1 9 H5' fill='none' stroke='${c}' stroke-width='1.3' stroke-linecap='round'/></svg>`; const gDown = c => `<svg width='10' height='11' viewBox='0 0 12 13' style='flex:none;'><path d='M6 1 V8.4' fill='none' stroke='${c}' stroke-width='1.4' stroke-linecap='round'/><path d='M3.2 5.8 L6 8.8 L8.8 5.8' fill='none' stroke='${c}' stroke-width='1.4' stroke-linecap='round' stroke-linejoin='round'/><path d='M1.6 11.4 H10.4' fill='none' stroke='${c}' stroke-width='1.4' stroke-linecap='round'/></svg>`; const gTyre = c => `<svg width='11' height='11' viewBox='0 0 12 12' style='flex:none;'><circle cx='6' cy='6' r='5' fill='none' stroke='${c}' stroke-width='1.4'/><circle cx='6' cy='6' r='1.8' fill='none' stroke='${c}' stroke-width='1.3'/></svg>`; const chips = []; const openings = [['binary_sensor.tesla_doors_open', 'doors'], ['binary_sensor.tesla_windows_open', 'windows'], ['binary_sensor.tesla_trunk_open', 'boot'], ['binary_sensor.tesla_frunk_open', 'frunk']].filter(o => S(o[0]) === 'on').map(o => o[1]); if (openings.length) { const t = openings.join(' · ') + ' open'; chips.push(chipH(gDoor(AMB), t.charAt(0).toUpperCase() + t.slice(1), AMB)); } const tp = [['fl', 'FL'], ['fr', 'FR'], ['rl', 'RL'], ['rr', 'RR']].filter(w => S('binary_sensor.tesla_tpms_warn_' + w[0]) === 'on').map(w => { const b = Number(S('sensor.tesla_tpms_' + w[0])); return w[1] + (isFinite(b) ? ' ' + b.toFixed(1) : ''); }); if (tp.length) { chips.push(chipH(gTyre(AMB), tp.join(' · ') + ' bar', AMB)); } if (S('binary_sensor.tesla_climate_on') === 'on' || S('binary_sensor.tesla_preconditioning') === 'on') { chips.push(chipH(gWind(CYN), S('binary_sensor.tesla_preconditioning') === 'on' ? 'Preconditioning' : 'Climate on', CYN)); } if (S('binary_sensor.tesla_sentry') === 'on') { chips.push(chipH(gEye(BLU), 'Sentry', BLU)); } if (S('binary_sensor.tesla_update_available') === 'on') { const ip = Number(S('sensor.tesla_install_perc')); chips.push(chipH(gDown(BLU), isFinite(ip) && ip > 0 && ip < 100 ? 'Installing ' + Math.round(ip) + '%' : 'Update ready', BLU)); } const alerts = chips.length ? `<div style='display:flex;flex-wrap:wrap;gap:6px;margin-top:-1px;opacity:${stale ? 0.45 : 1};'>${chips.join('')}</div>` : ''; const notch = isFinite(lim) && lim > 0 && lim < 100 ? `<div style='position:absolute;top:-2px;height:8px;width:2px;border-radius:1px;left:calc(${Math.round(lim)}% - 1px);background:rgba(255,255,255,0.55);'></div>` : ''; 
/* ---- inset-b "Away puck": round map puck shown only when the car is away; garage state lives in the header ---- */
const AMB2 = 'rgba(255,193,7,0.92)';
const num = v => { const n = Number(v); return isFinite(n) ? n : null; };
const attr = (id, k) => { const s = states[id]; return s && s.attributes ? s.attributes[k] : undefined; };
const trk = states['device_tracker.tesla_location'];
const trkSt = trk && !['unknown','unavailable',''].includes(String(trk.state)) ? String(trk.state) : null;
const cLat = num(attr('device_tracker.tesla_location','latitude')); const cLon = num(attr('device_tracker.tesla_location','longitude'));
const hLat = num(attr('zone.home','latitude')); const hLon = num(attr('zone.home','longitude'));
const hasPos = trkSt !== null && cLat !== null && cLon !== null && Math.abs(cLat) <= 85;
const away = hasPos && trkSt !== 'home';
let distM = null; if (hasPos && hLat !== null && hLon !== null) { const R = 6371000; const dp = (cLat - hLat) * Math.PI / 180; const dl = (cLon - hLon) * Math.PI / 180; const a = Math.sin(dp / 2) ** 2 + Math.cos(hLat * Math.PI / 180) * Math.cos(cLat * Math.PI / 180) * Math.sin(dl / 2) ** 2; distM = 2 * R * Math.asin(Math.sqrt(a)); }
const distTxt = distM === null ? '' : (distM < 950 ? Math.round(distM / 10) * 10 + ' m' : (distM < 9950 ? (distM / 1000).toFixed(1) + ' km' : Math.round(distM / 1000) + ' km'));
const zoneName = trkSt && !['home','not_home'].includes(trkSt) ? trkSt.replace(/_/g, ' ') : '';
const heading = num(S('sensor.tesla_heading'));
const driving = st === 'driving';
/* garage door: on = open. Quiet (nothing rendered) when closed or unknown. */
const gd = states['binary_sensor.garage_door'];
const gOpen = !!gd && gd.state === 'on';
let gFor = ''; if (gOpen && gd.last_changed && !isNaN(new Date(gd.last_changed))) { const m = Math.max(0, Math.round((now - new Date(gd.last_changed).getTime()) / 60000)); gFor = m < 60 ? m + ' min' : (m < 1440 ? Math.floor(m / 60) + ' h' + (m % 60 ? ' ' + (m % 60) : '') : Math.floor(m / 1440) + ' d'); }
const gGar = c => `<svg width='11' height='10' viewBox='0 0 13 12' style='flex:none;margin-right:4px;'><path d='M1 11 V4.6 L6.5 1.2 L12 4.6 V11' fill='none' stroke='${c}' stroke-width='1.4' stroke-linejoin='round' stroke-linecap='round'/><path d='M3.6 11 V6.8 H9.4 V11 M3.6 8.9 H9.4' fill='none' stroke='${c}' stroke-width='1.3' stroke-linejoin='round'/></svg>`;
const garageHdr = gOpen ? `<span style='display:flex;align-items:center;color:${AMB2};white-space:nowrap;font-variant-numeric:tabular-nums;'>${gGar(AMB2)}Garage open${gFor ? ' · ' + gFor : ''}</span>${lockTxt ? `<span style='color:rgba(255,255,255,0.22);margin:0 7px;'>|</span>` : ''}` : '';
/* location row in the info column (the geofence pin slot, which never shows today), only when away */
const locRow = away && (zoneName || distTxt) ? rowH(pin(dimCol), zoneName && distTxt ? zoneName + ' · ' + distTxt : (zoneName || distTxt), dimCol) : '';
const info2 = `<div style='display:flex;flex-direction:column;gap:4px;opacity:${stale ? 0.45 : 1};'>${locRow}${rows}</div>`;
/* map puck: OpenStreetMap raster tiles as <img>, darkened by CSS filter, mosaic positioned about the puck centre */
const TILE = 'https://tile.openstreetmap.org/';
const mapInset = (lat, lon, z, hw, hh) => { const n = 256 * Math.pow(2, z); const px = (lon + 180) / 360 * n; const la = lat * Math.PI / 180; const py = (1 - Math.log(Math.tan(la) + 1 / Math.cos(la)) / Math.PI) / 2 * n; let im = ''; for (let ty = Math.floor((py - hh) / 256); ty <= Math.floor((py + hh) / 256); ty++) { for (let tx = Math.floor((px - hw) / 256); tx <= Math.floor((px + hw) / 256); tx++) { if (ty < 0 || ty >= n / 256) { continue; } const wx = ((tx % (n / 256)) + n / 256) % (n / 256); im += `<img src='${TILE}${z}/${wx}/${ty}.png' alt='' style='position:absolute;left:${(tx * 256 - px).toFixed(1)}px;top:${(ty * 256 - py).toFixed(1)}px;width:256px;height:256px;max-width:none;display:block;'>`; } } return `<div style='position:absolute;left:50%;top:50%;width:0;height:0;filter:invert(1) hue-rotate(180deg) brightness(.75) contrast(.9) saturate(.3);'>${im}</div>`; };
const mkCol = chg ? GREEN : (driving ? 'rgba(66,165,245,0.95)' : 'rgba(255,255,255,0.92)');
const marker = driving && heading !== null ? `<svg width='14' height='14' viewBox='0 0 14 14' style='position:absolute;left:50%;top:50%;margin:-7px 0 0 -7px;transform:rotate(${Math.round(heading)}deg);'><path d='M7 1.2 L11.2 11.4 L7 9 L2.8 11.4 Z' fill='${mkCol}' stroke='#161616' stroke-width='1.2' stroke-linejoin='round'/></svg>` : `<div style='position:absolute;left:50%;top:50%;width:8px;height:8px;margin:-4px 0 0 -4px;border-radius:50%;background:${mkCol};box-shadow:0 0 0 2px #161616,0 0 0 3px rgba(255,255,255,0.28);'></div>`;
const osm = `<div style='position:absolute;left:0;right:0;bottom:3px;text-align:center;font-size:7.5px;line-height:1;color:rgba(255,255,255,0.28);letter-spacing:0.02em;'>©OSM</div>`;
const puck = away ? `<div style='flex:none;width:52px;height:52px;position:relative;overflow:hidden;border-radius:50%;border:1px solid rgba(255,255,255,0.10);background:#0f0f0f;margin-left:2px;margin-right:auto;opacity:${stale ? 0.45 : 1};'>${mapInset(cLat, cLon, driving ? 15 : 16, 28, 28)}${marker}${osm}</div>` : '';
return `<div style='display:flex;flex-direction:column;gap:9px;text-align:left;'><div style='display:flex;justify-content:space-between;align-items:center;'><span style='display:flex;align-items:center;gap:6px;'>${carGlyph}${boltG}<span style='font-size:10.5px;letter-spacing:0.10em;text-transform:uppercase;color:rgba(255,255,255,0.40);'>Tesla</span></span><span style='display:flex;align-items:center;font-size:11px;color:${lockCol};'>${garageHdr}${lockGlyph}${lockTxt}</span></div><div style='display:flex;align-items:center;justify-content:space-between;gap:10px;'><div style='flex:none;'><div style='font-size:36px;font-weight:600;line-height:1;color:${heroCol};font-variant-numeric:tabular-nums;'>${ok ? Math.round(socN) : '—'}<span style='font-size:17px;font-weight:400;color:rgba(255,255,255,0.45);'>%</span></div><div style='font-size:12px;color:${dim};margin-top:5px;'>${mi !== null ? mi + ' mi' : ''}</div></div>${puck}${info2}</div><div style='position:relative;height:4px;border-radius:2px;background:rgba(255,255,255,0.14);'><div style='position:absolute;left:0;top:0;bottom:0;width:${soc}%;border-radius:2px;background:${barCol};opacity:${stale ? 0.5 : 1};'></div>${notch}</div><div style='min-height:15px;display:flex;flex-wrap:wrap;align-items:flex-end;justify-content:space-between;column-gap:12px;row-gap:3px;'><div style='flex:0 1 auto;min-width:0;overflow:hidden;text-overflow:ellipsis;white-space:nowrap;font-size:12px;line-height:1.3;color:${l1c};'>${l1}</div>${l2 ? `<div style='flex:none;white-space:nowrap;font-size:11.5px;line-height:1.3;color:rgba(255,255,255,0.40);'>${l2}</div>` : ''}</div></div>${alerts}</div>`;
