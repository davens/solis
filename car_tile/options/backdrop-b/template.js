const S = id => (states[id] || {}).state; const OD = Object.keys(states).filter(k => k.includes('.octopus_energy_') && k.includes('_intelligent_')).map(k => k.slice(k.indexOf('.') + 1, k.indexOf('_intelligent_'))).sort()[0]; const g = sfx => { for (const d of ['sensor','number','time','select','binary_sensor']) { const s = states[d + '.' + OD + '_intelligent_' + sfx]; if (s && s.state !== 'unavailable' && s.state !== 'unknown') { return s; } } return null; }; const st = S('sensor.tesla_state') || 'unknown'; const cs = S('sensor.tesla_charging_state'); const kw = Number(S('sensor.tesla_charger_power')); const chg = cs === 'Charging' || (isFinite(kw) && kw > 0); const plugged = S('binary_sensor.tesla_plugged_in') === 'on'; const lockedS = S('binary_sensor.tesla_locked'); const socN = Number(S('sensor.tesla_battery')); const ok = isFinite(socN); const resting = ['offline','asleep','suspended'].includes(st); const stale = S('binary_sensor.teslamate_healthy') === 'off' || ['unknown','unavailable'].includes(st) || !ok; const soc = ok ? Math.max(0, Math.min(100, socN)) : 0; const km = Number(S('sensor.tesla_range')); const spd = Number(S('sensor.tesla_speed')); const spdTxt = isFinite(spd) && spd > 0 ? Math.round(spd * 0.621371) + ' mph' : ''; const mi = isFinite(km) ? Math.round(km * 0.621371) : null; const limS = g('charge_target') || states['sensor.tesla_charge_limit']; const lim = limS ? Number(limS.state) : NaN; const byS = g('target_time'); const by = byS ? String(byS.state).slice(0, 5) : null; const disp = g('dispatching'); const da = (disp && disp.attributes) || {}; const fmt = t => { const d = new Date(t); return isNaN(d) ? null : d.toTimeString().slice(0, 5); }; const plans = Array.isArray(da.planned_dispatches) ? da.planned_dispatches : []; const now = Date.now(); const raw = plans.filter(p => p && p.start && p.end && !isNaN(new Date(p.start)) && !isNaN(new Date(p.end)) && new Date(p.end).getTime() > now).sort((a, b) => new Date(a.start) - new Date(b.start)); const runs = []; for (const p of raw) { const last = runs[runs.length - 1]; if (last && new Date(p.start).getTime() - new Date(last.end).getTime() <= 60000) { if (new Date(p.end).getTime() > new Date(last.end).getTime()) { last.end = p.end; } } else { runs.push({ start: p.start, end: p.end }); } } const future = runs.slice(0, 2); if (!future.length && da.next_start && da.next_end && !isNaN(new Date(da.next_start)) && new Date(da.next_end).getTime() > now) { future.push({ start: da.next_start, end: da.next_end }); } const GREEN = 'rgb(76,175,80)'; const RED = 'rgb(244,67,54)'; const dim = 'rgba(255,255,255,0.45)'; const barCol = ok && soc <= 20 ? RED : GREEN; const asleep = resting; let l1 = ''; let l1c = dim; let l2 = ''; const ttf = Number(S('sensor.tesla_time_to_full_charge')); let left = ''; if (isFinite(ttf) && ttf > 0.02) { if (ttf < 1) { left = Math.round(ttf * 60) + ' min remaining'; } else { left = Math.floor(ttf) + ' h ' + Math.round((ttf % 1) * 60) + ' min remaining'; } } const add = Number(S('sensor.tesla_charge_energy_added')); if (stale) { l1 = 'No link to TeslaMate'; l1c = 'rgba(244,67,54,0.85)'; } else if (chg) { l1 = 'Charging' + (isFinite(kw) && kw > 0 ? ' · ' + kw.toFixed(1) + ' kW' : '') + (left ? ' · ' + left : ''); l1c = GREEN; if (isFinite(add) && isFinite(lim) && by) { l2 = '+' + add.toFixed(1) + ' kWh · → ' + Math.round(lim) + '% by ' + by; } } else if (plugged && cs === 'Complete') { l1 = 'Charge complete'; if (isFinite(add) && add > 0) { l2 = '+' + add.toFixed(1) + ' kWh added'; } } else if (plugged) { l1 = 'Plugged in' + (asleep ? ' · Asleep' : ' · Parked'); if (isFinite(lim) && by) { l2 = '→ ' + Math.round(lim) + '% by ' + by; } } else if (st === 'driving') { l1 = 'Driving' + (spdTxt ? ' · ' + spdTxt : ''); } else if (asleep) { l1 = 'Asleep'; } else { l1 = 'Parked'; } const lockTxt = lockedS === 'on' ? 'Locked' : (lockedS === 'off' ? 'Unlocked' : ''); const lockCol = lockedS === 'off' ? 'rgba(255,193,7,0.85)' : 'rgba(255,255,255,0.40)'; const lockGlyph = lockTxt ? `<svg width='9' height='11' viewBox='0 0 10 12' style='margin-right:4px;'><rect x='1' y='5.5' width='8' height='6' rx='1.5' fill='${lockCol}'/><path d='M 3 5.5 V 3.4 A 2 2 0 0 1 7 3.4 ${lockedS === 'off' ? '' : 'V 5.5'}' fill='none' stroke='${lockCol}' stroke-width='1.4'/></svg>` : ''; const heroCol = stale ? 'rgba(255,255,255,0.55)' : 'rgba(255,255,255,0.95)'; const carCol = chg ? GREEN : 'rgba(255,255,255,0.75)'; const carGlyph = `<svg width='19' height='10' viewBox='0 0 26 13' style='flex:none;display:block;'><path d='M1.5 9 C1.5 7 2.5 6.2 4.5 5.8 C6.5 5.4 8 5.2 9.5 5.1 C10.5 3.3 12 2.3 14 2.2 C16 2.2 18 3 20.5 4.6 C22.5 5 24 5.8 24.5 7 C24.8 7.8 24.8 8.5 24.6 9' fill='none' stroke='${carCol}' stroke-width='1.6' stroke-linecap='round'/><circle cx='6.5' cy='10.5' r='1.9' fill='none' stroke='${carCol}' stroke-width='1.4'/><circle cx='19.5' cy='10.5' r='1.9' fill='none' stroke='${carCol}' stroke-width='1.4'/></svg>`; const boltG = chg ? `<svg width='7' height='10' viewBox='0 0 8 12' style='flex:none;'><path d='M4.5 0.5 L1 6.5 H3.5 L2.5 11.5 L7 5 H4 L5.5 0.5 Z' fill='${GREEN}'/></svg>` : ''; const dimCol = 'rgba(255,255,255,0.50)'; const rowH = (glyph, txt, col) => `<div style='display:flex;align-items:center;justify-content:flex-end;gap:5px;font-size:11.5px;line-height:1.3;color:${col};white-space:nowrap;font-variant-numeric:tabular-nums;'>${glyph}<span>${txt}</span></div>`; const pin = c => `<svg width='10' height='11' viewBox='0 0 12 14' style='flex:none;'><path d='M6 1 C8.8 1 11 3.2 11 6 C11 9.5 6 13 6 13 C6 13 1 9.5 1 6 C1 3.2 3.2 1 6 1 Z' fill='none' stroke='${c}' stroke-width='1.5'/><circle cx='6' cy='6' r='1.6' fill='${c}'/></svg>`; const thermo = c => `<svg width='9' height='12' viewBox='0 0 10 14' style='flex:none;'><path d='M3.5 8.2 V2.5 A1.5 1.5 0 0 1 6.5 2.5 V8.2 A3.2 3.2 0 1 1 3.5 8.2 Z' fill='none' stroke='${c}' stroke-width='1.4'/><circle cx='5' cy='10.6' r='1.4' fill='${c}'/></svg>`; const clockG = c => `<svg width='10' height='10' viewBox='0 0 12 12' style='flex:none;'><circle cx='6' cy='6' r='5' fill='none' stroke='${c}' stroke-width='1.4'/><path d='M6 3.5 V6 L8 7.5' fill='none' stroke='${c}' stroke-width='1.4' stroke-linecap='round'/></svg>`; 
/* --- location, garage door (new) --- */
const geoOf = id => { const s = states[id]; const a = s && s.attributes; if (!a) { return null; } const la = Number(a.latitude); const lo = Number(a.longitude); return isFinite(la) && isFinite(lo) ? { lat: la, lon: lo } : null; };
const trk = S('device_tracker.tesla_location'); const trkKnown = !!trk && !['unknown', 'unavailable', ''].includes(trk);
const car = trkKnown ? geoOf('device_tracker.tesla_location') : null; const home = geoOf('zone.home');
const isHome = trk === 'home'; const away = trkKnown && !isHome; const zoneName = away && trk !== 'not_home' ? trk.charAt(0).toUpperCase() + trk.slice(1) : '';
const R2 = Math.PI / 180;
const hav = (a, b) => { const dl = (b.lat - a.lat) * R2; const dn = (b.lon - a.lon) * R2; const h = Math.sin(dl / 2) ** 2 + Math.cos(a.lat * R2) * Math.cos(b.lat * R2) * Math.sin(dn / 2) ** 2; return 12742000 * Math.asin(Math.sqrt(h)); };
const brg = (a, b) => { const y = Math.sin((b.lon - a.lon) * R2) * Math.cos(b.lat * R2); const x = Math.cos(a.lat * R2) * Math.sin(b.lat * R2) - Math.sin(a.lat * R2) * Math.cos(b.lat * R2) * Math.cos((b.lon - a.lon) * R2); return ((Math.atan2(y, x) / R2) + 360) % 360; };
const compass = d => ['N', 'NE', 'E', 'SE', 'S', 'SW', 'W', 'NW'][Math.round(d / 45) % 8];
const distM = car && home ? hav(home, car) : NaN;
const fmtDist = m => { const mi = m / 1609.344; return mi < 10 ? mi.toFixed(1) + ' mi' : Math.round(mi) + ' mi'; };
const distTxt = isFinite(distM) ? fmtDist(distM) + ' ' + compass(brg(home, car)) : '';
const locTxt = away ? (zoneName ? zoneName + (distTxt ? ' · ' + distTxt : '') : distTxt) : '';
const hdg = Number(S('sensor.tesla_heading'));
const gdS = states['binary_sensor.garage_door']; const gOpen = !!gdS && gdS.state === 'on';
const gMin = gOpen ? Math.round((now - new Date(gdS.last_changed || NaN).getTime()) / 60000) : NaN;
const gDur = !isFinite(gMin) || gMin < 1 ? '' : (gMin < 60 ? gMin + ' min' : (gMin < 600 ? Math.floor(gMin / 60) + ' h' + (gMin % 60 ? ' ' + (gMin % 60) + ' min' : '') : Math.round(gMin / 60) + ' h'));
const gTxt = gOpen ? 'Garage open' + (gDur ? ' · ' + gDur : '') : '';
const gGar = c => `<svg width='11' height='11' viewBox='0 0 13 13' style='flex:none;'><path d='M1 12 V5.2 L6.5 1.4 L12 5.2 V12' fill='none' stroke='${c}' stroke-width='1.4' stroke-linejoin='round' stroke-linecap='round'/><path d='M3.6 12 V7 H9.4 V12' fill='none' stroke='${c}' stroke-width='1.4' stroke-linejoin='round'/><path d='M3.6 9.5 H9.4' fill='none' stroke='${c}' stroke-width='1.2' stroke-linecap='round'/></svg>`;
/* tiles: OpenStreetMap standard (keyless, 256 px, z/x/y), inverted to dark by a CSS filter on the mosaic */
const tileUrl = (z, x, y) => `https://tile.openstreetmap.org/${z}/${x}/${y}.png`;
const tileFilter = 'invert(1) hue-rotate(180deg) brightness(0.55) contrast(0.95) saturate(0.35)';
const osmCredit = `<div style='position:absolute;right:5px;bottom:3px;font-size:8px;line-height:1;color:rgba(255,255,255,0.28);letter-spacing:0.01em;white-space:nowrap;pointer-events:none;'>© OpenStreetMap</div>`;
/* map maths: Web-Mercator world pixels at zoom z; 256 css px tiles */
const wpx = (p, z) => { const n = 256 * Math.pow(2, z); const s = Math.sin(p.lat * R2); return { x: (p.lon + 180) / 360 * n, y: (0.5 - Math.log((1 + s) / (1 - s)) / (4 * Math.PI)) * n }; };
const mpp = (lat, z) => 156543.03 * Math.cos(lat * R2) / Math.pow(2, z);
const mosaic = (c, z, rect) => { const w = wpx(c, z); let h = ''; const tx0 = Math.floor((w.x + rect.x0) / 256); const tx1 = Math.floor((w.x + rect.x1) / 256); const ty0 = Math.floor((w.y + rect.y0) / 256); const ty1 = Math.floor((w.y + rect.y1) / 256); const nmax = Math.pow(2, z); for (let ty = ty0; ty <= ty1; ty++) { if (ty < 0 || ty >= nmax) { continue; } for (let tx = tx0; tx <= tx1; tx++) { const txw = ((tx % nmax) + nmax) % nmax; h += `<img src='${tileUrl(z, txw, ty)}' alt='' draggable='false' style='position:absolute;left:${(tx * 256 - w.x).toFixed(1)}px;top:${(ty * 256 - w.y).toFixed(1)}px;width:256px;height:256px;display:block;'>`; } } return h; };
const homeRing = (c, z) => { if (!home) { return ''; } const w = wpx(c, z); const hw = wpx(home, z); const r = Math.max(6, 100 / mpp(home.lat, z)); const dx = hw.x - w.x; const dy = hw.y - w.y; if (Math.abs(dx) > 700 || Math.abs(dy) > 400) { return ''; } return `<div style='position:absolute;left:${(dx - r).toFixed(1)}px;top:${(dy - r).toFixed(1)}px;width:${(2 * r).toFixed(1)}px;height:${(2 * r).toFixed(1)}px;border-radius:50%;border:1px solid rgba(255,255,255,0.20);background:rgba(255,255,255,0.03);'></div>`; };
const marker = () => { const drv = st === 'driving'; const col = drv ? 'rgba(255,255,255,0.95)' : (chg ? GREEN : 'rgba(255,255,255,0.92)'); if (drv && isFinite(hdg)) { return `<div style='position:absolute;left:-8px;top:-8px;width:16px;height:16px;'><svg width='16' height='16' viewBox='0 0 16 16' style='display:block;transform:rotate(${Math.round(hdg)}deg);'><path d='M8 1.5 L13 13.5 L8 10.8 L3 13.5 Z' fill='${col}' stroke='rgba(0,0,0,0.75)' stroke-width='1.2' stroke-linejoin='round'/></svg></div>`; } return `<div style='position:absolute;left:-5.5px;top:-5.5px;width:11px;height:11px;border-radius:50%;background:${col};box-shadow:0 0 0 2.5px rgba(0,0,0,0.85), 0 0 0 6px rgba(255,255,255,0.14);'></div>`; };
let rows = ''; const geo = S('sensor.tesla_geofence'); if (geo && !['unknown','unavailable','none',''].includes(String(geo).trim().toLowerCase())) { rows += rowH(pin(dimCol), geo, dimCol); } if (locTxt) { rows += rowH(pin(dimCol), locTxt, dimCol); }
const ti = Number(S('sensor.tesla_inside_temp')); if (isFinite(ti)) { rows += rowH(thermo(dimCol), ti.toFixed(1) + '°', dimCol); } if (plugged) { if (future.length) { const live = new Date(future[0].start).getTime() <= now; const scol = live ? 'rgba(76,175,80,0.95)' : dimCol; rows += rowH(clockG(scol), future.map(p => fmt(p.start) + '–' + fmt(p.end)).join(' · '), scol); } else { rows += rowH(clockG(dimCol), 'no slots', dimCol); } } const info = `<div style='display:flex;flex-direction:column;gap:4px;opacity:${stale ? 0.45 : 1};'>${rows}</div>`; const AMB = 'rgba(255,193,7,0.92)'; const BLU = 'rgba(66,165,245,0.92)'; const CYN = 'rgba(38,198,218,0.92)'; const tint = c => c.replace('0.92', '0.13'); const chipH = (glyph, txt, col) => `<span style='display:inline-flex;align-items:center;gap:5px;padding:3px 8px;border-radius:6px;background:${tint(col)};color:${col};font-size:11px;line-height:1.25;white-space:nowrap;'>${glyph}<span>${txt}</span></span>`; const gDoor = c => `<svg width='10' height='11' viewBox='0 0 12 13' style='flex:none;'><path d='M1.2 12 V3.2 L8.2 1 V12' fill='none' stroke='${c}' stroke-width='1.4' stroke-linejoin='round'/><circle cx='6.6' cy='7' r='0.9' fill='${c}'/><path d='M9.8 12 H11.2' stroke='${c}' stroke-width='1.4' stroke-linecap='round'/></svg>`; const gEye = c => `<svg width='12' height='10' viewBox='0 0 14 11' style='flex:none;'><path d='M1 5.5 C3.2 2 10.8 2 13 5.5 C10.8 9 3.2 9 1 5.5 Z' fill='none' stroke='${c}' stroke-width='1.3'/><circle cx='7' cy='5.5' r='1.7' fill='${c}'/></svg>`; const gWind = c => `<svg width='12' height='10' viewBox='0 0 13 11' style='flex:none;'><path d='M1 3 H7.4 A1.7 1.7 0 1 0 5.7 1.3' fill='none' stroke='${c}' stroke-width='1.3' stroke-linecap='round'/><path d='M1 6 H9.4 A1.7 1.7 0 1 1 7.7 7.7' fill='none' stroke='${c}' stroke-width='1.3' stroke-linecap='round'/><path d='M1 9 H5' fill='none' stroke='${c}' stroke-width='1.3' stroke-linecap='round'/></svg>`; const gDown = c => `<svg width='10' height='11' viewBox='0 0 12 13' style='flex:none;'><path d='M6 1 V8.4' fill='none' stroke='${c}' stroke-width='1.4' stroke-linecap='round'/><path d='M3.2 5.8 L6 8.8 L8.8 5.8' fill='none' stroke='${c}' stroke-width='1.4' stroke-linecap='round' stroke-linejoin='round'/><path d='M1.6 11.4 H10.4' fill='none' stroke='${c}' stroke-width='1.4' stroke-linecap='round'/></svg>`; const gTyre = c => `<svg width='11' height='11' viewBox='0 0 12 12' style='flex:none;'><circle cx='6' cy='6' r='5' fill='none' stroke='${c}' stroke-width='1.4'/><circle cx='6' cy='6' r='1.8' fill='none' stroke='${c}' stroke-width='1.3'/></svg>`; const chips = []; const openings = [['binary_sensor.tesla_doors_open', 'doors'], ['binary_sensor.tesla_windows_open', 'windows'], ['binary_sensor.tesla_trunk_open', 'boot'], ['binary_sensor.tesla_frunk_open', 'frunk']].filter(o => S(o[0]) === 'on').map(o => o[1]); if (openings.length) { const t = openings.join(' · ') + ' open'; chips.push(chipH(gDoor(AMB), t.charAt(0).toUpperCase() + t.slice(1), AMB)); } const tp = [['fl', 'FL'], ['fr', 'FR'], ['rl', 'RL'], ['rr', 'RR']].filter(w => S('binary_sensor.tesla_tpms_warn_' + w[0]) === 'on').map(w => { const b = Number(S('sensor.tesla_tpms_' + w[0])); return w[1] + (isFinite(b) ? ' ' + b.toFixed(1) : ''); }); if (tp.length) { chips.push(chipH(gTyre(AMB), tp.join(' · ') + ' bar', AMB)); } if (S('binary_sensor.tesla_climate_on') === 'on' || S('binary_sensor.tesla_preconditioning') === 'on') { chips.push(chipH(gWind(CYN), S('binary_sensor.tesla_preconditioning') === 'on' ? 'Preconditioning' : 'Climate on', CYN)); } if (S('binary_sensor.tesla_sentry') === 'on') { chips.push(chipH(gEye(BLU), 'Sentry', BLU)); } if (S('binary_sensor.tesla_update_available') === 'on') { const ip = Number(S('sensor.tesla_install_perc')); chips.push(chipH(gDown(BLU), isFinite(ip) && ip > 0 && ip < 100 ? 'Installing ' + Math.round(ip) + '%' : 'Update ready', BLU)); } const alerts = chips.length ? `<div style='display:flex;flex-wrap:wrap;gap:6px;margin-top:-1px;opacity:${stale ? 0.45 : 1};'>${chips.join('')}</div>` : ''; const notch = isFinite(lim) && lim > 0 && lim < 100 ? `<div style='position:absolute;top:-2px;height:8px;width:2px;border-radius:1px;left:calc(${Math.round(lim)}% - 1px);background:rgba(255,255,255,0.55);'></div>` : ''; 
/* --- map backdrop B: right-side fade, only while away --- */
const MZ = 14;
const gHdr = gTxt ? `<span style='display:flex;align-items:center;gap:5px;color:${AMB};white-space:nowrap;'>${gGar(AMB)}<span>${gTxt}</span></span>` : '';
let mapHtml = '';
if (car && away) {
  const BW = 62; /* map box width, % of tile */ const AX = 0.42; const AY = 66;
  const tiles = mosaic(car, MZ, { x0: -140, x1: 190, y0: -AY - 4, y1: 150 });
  const layer = `<div style='position:absolute;left:${Math.round(AX * 100)}%;top:${AY}px;width:0;height:0;'><div style='position:absolute;left:0;top:0;width:0;height:0;filter:${tileFilter};'>${tiles}</div>${homeRing(car, MZ)}${marker()}</div>`;
  const scrimX = `<div style='position:absolute;inset:0;background:linear-gradient(90deg, rgba(22,22,22,1) 0%, rgba(22,22,22,0.75) 28%, rgba(22,22,22,0.32) 55%, rgba(22,22,22,0.30) 78%, rgba(22,22,22,0.60) 100%);'></div>`;
  const scrimY = `<div style='position:absolute;inset:0;background:linear-gradient(180deg, rgba(22,22,22,0.60) 0%, rgba(22,22,22,0.05) 34%, rgba(22,22,22,0.05) 60%, rgba(22,22,22,0.90) 100%);'></div>`;
  mapHtml = `<div style='position:absolute;top:0;bottom:0;right:0;width:${BW}%;overflow:hidden;opacity:${stale ? 0.5 : 1};'>${layer}${scrimX}${scrimY}${osmCredit}</div>`;
}
const rootStyle = 'position:relative;overflow:hidden;margin:-14px -16px -13px;padding:14px 16px 13px;border-radius:11px;';
return `<div style='${rootStyle}'>${mapHtml}<div style='position:relative;z-index:1;${mapHtml ? 'text-shadow:0 0 5px rgba(22,22,22,0.9), 0 1px 3px rgba(22,22,22,0.9);' : ''}'><div style='display:flex;flex-direction:column;gap:9px;text-align:left;'><div style='display:flex;justify-content:space-between;align-items:center;'><span style='display:flex;align-items:center;gap:6px;'>${carGlyph}${boltG}<span style='font-size:10.5px;letter-spacing:0.10em;text-transform:uppercase;color:rgba(255,255,255,0.40);'>Tesla</span></span><span style='display:flex;align-items:center;gap:10px;font-size:11px;'>${gHdr}<span style='display:flex;align-items:center;color:${lockCol};'>${lockGlyph}${lockTxt}</span></span></div><div style='display:flex;align-items:center;justify-content:space-between;gap:10px;'><div style='flex:none;'><div style='font-size:36px;font-weight:600;line-height:1;color:${heroCol};font-variant-numeric:tabular-nums;'>${ok ? Math.round(socN) : '—'}<span style='font-size:17px;font-weight:400;color:rgba(255,255,255,0.45);'>%</span></div><div style='font-size:12px;color:${dim};margin-top:5px;'>${mi !== null ? mi + ' mi' : ''}</div></div>${info}</div><div style='position:relative;height:4px;border-radius:2px;background:rgba(255,255,255,0.14);'><div style='position:absolute;left:0;top:0;bottom:0;width:${soc}%;border-radius:2px;background:${barCol};opacity:${stale ? 0.5 : 1};'></div>${notch}</div><div style='min-height:15px;display:flex;flex-wrap:wrap;align-items:flex-end;justify-content:space-between;column-gap:12px;row-gap:3px;'><div style='flex:0 1 auto;min-width:0;overflow:hidden;text-overflow:ellipsis;white-space:nowrap;font-size:12px;line-height:1.3;color:${l1c};'>${l1}</div>${l2 ? `<div style='flex:none;white-space:nowrap;font-size:11.5px;line-height:1.3;color:rgba(255,255,255,0.40);'>${l2}</div>` : ''}</div></div>${alerts}</div></div>`;
